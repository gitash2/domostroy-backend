apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "domostroy-helm.fullname" . }}-test-connection"
  labels:
    {{- include "domostroy-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "domostroy-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
