apiVersion: v1
kind: Service
metadata:
  name: {{ include "domostroy-helm.fullname" . }}
  labels:
    {{- include "domostroy-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "domostroy-helm.selectorLabels" . | nindent 4 }}
